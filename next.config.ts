import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  allowedDevOrigins: ["noble-champion-fighter-consumer.trycloudflare.com"],
};

export default nextConfig;
